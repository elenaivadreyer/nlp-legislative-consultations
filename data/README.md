# Data

This directory contains data for the NLP project:

- **raw/**: Original, unprocessed data files (Excel, CSV, etc.)
- **processed/**: Cleaned and preprocessed data ready for modeling

## Note

Do not commit large data files to the repository. Use `.gitignore` to exclude data files if needed.
